/** @type {import('next').NextConfig} */
const nextConfig = {
  // This enables Tailwind CSS during the Vercel build process.
  // We don't need experimental features for this app.
};

module.exports = nextConfig;